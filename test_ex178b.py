__author__ = "Высоцкая И.Д."
'Модуль с тестами'

from func_ex178b import * # подключаем модуль с функциями

"""Функция с тестами"""
def test():
    assert count_ch(5, [1,2,3,4,5]) == 1
    assert count_ch(3, [3,15,18]) == 2 # какие-то кратны 3 и не кратны 5
    assert count_ch(3, [5, 15, 20]) == 0 # все кратны 5
    assert count_ch(3, [3, 21, 18]) == 3 # все кратны 3 и не кратны 5
    assert count_ch(1, [3]) == 1 # один элемент кратный 3 и не кратный 5